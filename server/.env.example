# Sao chép thành server/.env (KHÔNG commit) và điền giá trị thật.
PORT=8787
# Bí mật ký token đăng nhập — ĐỔI chuỗi ngẫu nhiên khi deploy.
AUTH_SECRET=doi-chuoi-ngau-nhien-dai
PEXELS_KEY=
# UNSPLASH_KEY=   (tùy chọn, ảnh dự phòng)
# Chat AI nâng cấp (premium) — tương thích OpenAI. Để trống = chỉ chế độ kịch bản (free).
DEEPSEEK_API_KEY=
# DEEPSEEK_MODEL=deepseek-chat   (tùy chọn)
